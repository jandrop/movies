name: Dart CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    container:
      image:  google/dart:latest
    
    steps:
    - name: Flutter CLI
    - uses: steebchen/flutter@v1.0.0
    - name: Run tests
      run: pub run test
